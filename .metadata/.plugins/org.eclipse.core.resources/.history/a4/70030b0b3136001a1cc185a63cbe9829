package desafio_sprint_5;

import java.io.*;

public class arqs {

	public void escrever(pessoa p) throws IOException {
		//OutputStream fos = new FileOutputStream("Pessoas.txt");
        //Writer osw = new OutputStreamWriter(fos);
        //BufferedWriter bw = new BufferedWriter(osw);
        FileWriter fw = new FileWriter("Pessoas.txt");

        fw.write(p.getCodigo() + ";" + p.getNome() + ";" + p.getEndereco() + ";" + p.getIdade() +"\n");

        
        fw.close();
	}
	
	public void ler() {
		
	}
}
